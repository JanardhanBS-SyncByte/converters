# helm/

```
helm/converters/  →  kernel-bio-converter  :8079  /v1/converter-service
deploy/           →  install wrappers
```

**Rules:** no secrets in `values.yaml` · health path = context-path · bump `Chart.yaml`/README on publish
